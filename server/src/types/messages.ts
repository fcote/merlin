const BAD_INPUTS = 'Wrong inputs'
const ALREADY_EXISTS = (key: string) => `This ${key} is already taken`
const DOESNT_EXISTS = (key: string) => `This ${key} doesn't exists`

export { BAD_INPUTS, ALREADY_EXISTS, DOESNT_EXISTS }
